name: Foundry Local Web Server (C#)
description: Start a local OpenAI-compatible web server and call it from C#.

build: "dotnet restore ./*.csproj --source https://api.nuget.org/v3/index.json && dotnet build ./*.csproj --no-restore --verbosity minimal"

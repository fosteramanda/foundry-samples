name: agenticcolleague

hooks:
  preprovision:
    shell: pwsh
    run: .\scripts\preflight.ps1
    interactive: false
    continueOnError: false
  postprovision:
    shell: pwsh
    run: ./scripts/post-provision.ps1
    interactive: true
    continueOnError: false

# services:
#   web:
#     project: ./src
#     language: csharp
#     host: foundry.containeragent
    # hooks:
    #   # This hook runs before this specific service is built
    #   prebuild:
    #     posix:
    #       shell: sh
    #       run: ./service-prebuild.sh
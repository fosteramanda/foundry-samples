name: Voice Live Bridge LangChain Python
description: A tool-using LangChain hosted text agent implementing Voice Live Bridge Protocol 1.0.

build: >-
  python -m pip install --disable-pip-version-check
  -r src/voice-live-bridge-langchain-python/requirements.txt
  -r src/voice-live-bridge-langchain-python/requirements-dev.txt
validate: >-
  cd src/voice-live-bridge-langchain-python &&
  python -m compileall -q . &&
  python -c "import azure.ai.agentserver.invocations.voice" &&
  python -m ruff check .
test: >-
  cd src/voice-live-bridge-langchain-python &&
  python -m pytest -q
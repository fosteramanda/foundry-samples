FOUNDRY_PROJECT_ENDPOINT="..."
AZURE_AI_MODEL_DEPLOYMENT_NAME="..."

# Optional. TLS certificate verification for outbound requests (default: true).
# Set to "false" only when testing through a TLS-intercepting egress proxy
# ("Full inspection" mode) whose CA is not trusted by the container.
# EGRESS_TEST_VERIFY_TLS="true"
